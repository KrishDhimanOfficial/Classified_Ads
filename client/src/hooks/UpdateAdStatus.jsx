import React from 'react'

const UpdateAdStatus = () => {
    const update = async () => {
        try {
            const res = await Da
        } catch (error) {
            console.error(error)
        }
    }
    return (<>
        dsge
    </>)
}

export default UpdateAdStatus
